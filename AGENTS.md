# 模擬問題作成ルール

## 1. 基本方針
- **完全オリジナル問題の作成は厳禁**。必ず過去問（`kaisetu/` フォルダ等の過去問・解説）を参考・ベースとすること。
- **問番号の1対1対応の厳守**：
  - 模擬試験の【問N】は、必ず過去問（第56回〜第65回）の対応する【問N】をベースに作成する。
  - 勝手に問の順番や出題テーマをシャッフル・入れ替えないこと。
  - （例：専門知識の【問15】は、過去問10回すべてで出題されている「テレコネクション・全球大気海洋循環」を必ず出題する）。

## 2. 問題の構成と改変ルール
- **選択肢の構成数**：
  - 過去問が `(a)〜(d)` の4肢構成である問題は、必ず `(a)〜(d)` で作成すること。手抜きで `(a)〜(c)` に縮めない。
- **正誤判定問題**：
  - 過去問の文章の一部を変更して、「正」の文を「誤」に（または「誤」を「正」に）変更する。
  - 全問が「①（正正正）」や「②（正正誤）」など特定番号に偏らないよう、正解選択肢（①〜⑤）をバランスよく分散させる。
- **計算問題の扱い**：
  - 「計算問題4問」などの無理な枠にこだわらず、過去問に存在しない架空の計算問題（ドップラー速度のベクトル計算等）を一から創作しないこと。
  - 過去問に実在する計算問題（熱力学、地衡風、放射平衡、予報精度等）をベースに数値や条件を変えて作成する。
- **出題分野の固定**：
  - 一般知識の【問12】〜【問15】は、必ず「気象業務法」および関連法令（災害対策基本法等）を出題する。

## 3. フォーマットと見出しルール
- **選択肢と正誤表記の表記ルール【厳守】**：
  - 選択肢および解説では「○」「×」の記号は使用せず、**必ず「正」「誤」の文字（例：① 正 正 正 正）**を使用すること。

- **見出し統一**：各問の見出しは `【問1】`, `【問2】`, ... `【問15】` と表記する（簡体字「问」はパースエラーになるため絶対に使用しない）。
- **改行の徹底**：見出し `【問N】` の直前には必ず空行（改行）を入れ、問題同士が連結・合体しないようにする。
- **画像問題の扱い**：画像・図がある問題は、テキスト記述に変換してから変更を加える。テキスト化が困難な場合は無理に進めずユーザーに報告する。

## 4. 解答と解説の記述ルール
- 解説は短く分かりやすく記述する。
- **「誤」の選択肢についての解説【厳守】**：単に「〜は誤りである」と述べるだけの解説は厳禁。**どこが間違っていて、正しくはどうなのか（正しい数値・現象・メカニズム・法制度）**を明確に記述すること。
- **整合性の徹底**：問題用紙に記載した選択肢（①〜⑤）と、解説の判定結果および正解番号が100%完全に一致していること。

## 5. データ生成・反映フロー
- `mogi/[一般|専門]/模擬試験/[1|2|3]/` 配下に `問題用紙.md` と `解答と解説.md` を作成。
- 作成後、必ず `python3 generate_data.py` を実行して `data.js` に反映させ、15問15解説が正常に取り込まれたか確認する。
