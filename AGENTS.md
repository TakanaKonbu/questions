# 模擬問題作成ルール

## 1. 基本方針
- 一から完全にオリジナルの問題を制作することは**厳禁**。必ず過去問（`kaisetu/` フォルダ等の過去問・解説）を参考・ベースとすること。
- 参考にする過去問回を決め、その回の全15問の数値、言い回し、条件を変更して作成する。

## 2. 問題の作成ルール
- **正誤判定問題**: 選択肢の組み合わせを変える、文章の一部を変更して「正」の文を「誤」の文に（あるいは「誤」を「正」に）変更する。
- **フォーマット**: 選択肢の上のヘッダー行 `(a)(b)(c)` 等は入れない。
- **見出し統一**: 各問の見出しは `【問1】`, `【问2】` ... ではなく `【問1】`, `【問2】`, ... `【問15】` と表記する（簡体字「问」はパースエラーになるため絶対に使用しない）。

## 3. 画像問題の扱いと判定ルール
- 画像・図がある問題は、テキスト記述に変換してから変更を加える。
- **【重要報告ルール】**: 画像問題でどうしてもテキストに直すことができない場合、またはテキストに直すと問題として欠陥・不備が生じると判断した場合は、無理に模擬作成を進めず、**作成を一旦取りやめて先にユーザーへ報告・確認する**。

## 4. 解答と解説の記述ルール
- 解説は短く分かりやすく記述する。
- **【厳守】正誤判定問題の解説**: 特に「誤」の選択肢については、単に「〜は誤りである」と述べるだけの解説は厳禁。**どこが間違っていて、正しくはどうなのか（正しい数値・現象・メカニズム・法制度）**を明確に記述すること。

## 5. データ生成・反映フロー
- `mogi/[一般|専門]/模擬試験/[1|2|3...]/` 配下に `問題用紙.md` と `解答と解説.md` を作成。
- 作成後、必ず `python3 generate_data.py` を実行して `data.js` に反映させ、テストスクリプトで15問15解説が正常に取り込まれたか確認する。
